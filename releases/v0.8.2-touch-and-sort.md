# v0.8.2 — Touch double-tap, copy naming, round order
- Copy to New: name becomes "<base> - Mon D" (IST), stripping any prior date suffix.
- ResultsMatrix cells detect double-tap via 400ms click timing (works on iOS/Android where dblclick doesn't) + touch-manipulation to kill zoom delay.
- Matches tab: folding removed; groups sorted by round descending (ad-hoc matches on top), ✓ done marker retained on completed rounds.
