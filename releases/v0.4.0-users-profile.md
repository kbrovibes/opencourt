# v0.4.0 — Users tab + Profile

- Users tab (admin only): open-event dropdown selector scopes all actions; per-player Check in / Undo / Register / Withdraw; search; checked-in-first sort; add single user (name + optional email for later login-linking); bulk add via textarea ("Name" or "Name, email" per line, dedup by name).
- Profile tab: avatar initials, tap-to-edit display name (PATCH /api/me), email, admin badge, three-way theme toggle, logout, app version footer.
