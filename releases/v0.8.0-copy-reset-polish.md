# v0.8.0 — Copy to New, resets, confirmations, matches-tab polish
- oc_events.copied_from column; POST /api/events/[id]/copy (draft, today in IST, fresh short code).
- CopyCheckins widget (folded) on copied events in roster stage: source roster as tap-cards, POST /api/events/[id]/checkin-bulk.
- POST /api/events/[id]/reset {mode: scores|event}. resetAllScores() rewinds knockout slots for teams that advanced by winning while keeping bye/playoff seeds; reset event clears matches → teams_locked, keeps teams, revives completed events.
- ConfirmDialog component; wired to Back to Draft, Cancel, Reset Scores, Reset Event.
- Score edit on completed matches (event not completed); 400 when the next bracket round is already scored.
- STAGE_TRANSITIONS: matches_set → team_formation (clears matches) for direct team editing.
- MatchesSection: active rounds first, completed rounds folded with ✓ done headers.
- StagePanel de-chromed; stage chip beside status badge; nav: Today first, Users→Players.
