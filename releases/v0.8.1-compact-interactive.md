# v0.8.1 — Compact event page + interactive score grid
- ShareLink is now a chip in the event meta row (Web Share / clipboard); URL box removed.
- EventAdminControls: uniform grid-cols-3, py-1.5/text-xs buttons; Un-start moved here from StagePanel; labels shortened (↩︎ Draft, 🗑 Delete).
- ResultsMatrix: w-full when ≤5 teams (min-w cells grow); single-tap selects a cell (sky ring), double-tap opens ScoreDialog (prefilled, save → PATCH match, works for new and existing scores until event completed).
- TournamentVisuals is now a client component to support the interactivity.
