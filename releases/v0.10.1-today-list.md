# v0.10.1 — Today multi-event list
- getMyLiveEvents (active registrations on live events) + todayIST helper; layout picks "/" | direct event | /today by count.
- New /today page listing today's events with shared EventCard (extracted from home); redirects appropriately when 0/1.
