# v0.12.0 — Player profiles, sortable table, calendar, pagination
- VerifiedBadge (scalloped seal SVG) replaces the plain tick; used in players table, rosters, profiles.
- UsersAdminClient rebuilt as a sortable table (name/skill/type + actions col, EditPanel as expanding row, default sort = check-in status).
- /players/[id] read-only profile (getPlayer + getPlayerStats); name links from players table and event rosters via NavLink (no underline).
- EventsHomeClient: month calendar (event dots, today ring, selected fill, ‹ › month nav capped -12/+1), date filter with Clear, Past section paginated by 10 with See more; home page.tsx now a thin server fetch.
