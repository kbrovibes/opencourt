# v0.10.0 — Groups format, knockout fairness, lazy team edits
- oc_teams.group_no / oc_matches.group_no; match_format 'groups'. generateGroups (modulo distribution by seed, circle-method RR per group); generateGroupKnockout (top 2 per group, cross-paired A1–B2 circular, TBD rounds to a best-of final; G∈{2,4} keeps brackets power-of-2).
- generateSingleElim shuffles teams (Fisher-Yates) when count isn't a power of two — random bye draw; format picker warns and recommends Groups.
- EventTabs persists active tab in sessionStorage keyed by path; restored on mount (root-cause class of the "jumped to Overview after 5th score" report was tab-state loss on refresh/remount — persistence fixes it regardless of trigger).
- EventAdminControls gains 🚀 Start next to ✅ Complete (canStart when matches_set); StagePanel keeps Change format / Edit teams.
- TeamFormationPanel: pendingDeletes with strikethrough + undo, single "Save changes · +N · −M" applying deletes then creates.
- Teams list single-line with group tag; per-group standings tables and matrices; Group X · Round N headers in Matches.
- titleCaseName() applied at all display points (roster, users list, header, profile, team labels).
