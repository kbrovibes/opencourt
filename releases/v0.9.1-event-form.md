# v0.9.1 — Event form polish
- start_time now captured via <input type="time"> (stored HH:MM); formatStartTime renders 12-hour, to24h converts legacy freeform values for the edit form.
- All form controls standardized to h-11 (inputs + segmented toggles) fixing the side-by-side alignment glitch.
- LocationPicker: debounced Photon (OSM) autocomplete — keyless, CORS-friendly, bbox 72,7→85,19 (south India) with Chennai bias — plus up to 3 recently-used venue badges via getRecentLocations(). Swap to Google Places later requires only an API key + one fetch change.
