# v0.7.1 — Compact team names
- teamOpts gains shortNames (first name per player); Teams list and ResultsMatrix render one name per line.
- Matrix rows taller (h-10 cells, py-2 name cells, wider spacing) so the grid reads clearly on phones.
