# v0.6.2 — Completed-view dedupe + admin label
- Header shows a small "ADMIN" tag next to the avatar when the signed-in player is admin.
- Completed events: top banner is the single source of winner/runner-up/stats. Overview drops the progress bar and champion callouts; BracketView loses its 👑 champion column (the green final says it); podium removed; Standings is a plain table.
- While running: Overview keeps progress bar + a single compact champion line once a final is decided.
- Audited all mutation routes — every admin action (events CRUD/delete, stage, teams, match generation/scores, player create/edit/disable) is behind requireAdmin; only self register/check-in/partner and own-profile edits are self-service.
