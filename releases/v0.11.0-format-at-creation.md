# v0.11.0 — Format at creation, auto-complete, verified ticks, banner stats
- lib/formats.ts single catalog (no emojis); create/edit forms get a format select ("Choose later" = null); PATCH guards match_format once matches exist; StagePanel shows summary + Change when preselected.
- matches PATCH auto-completes the event (non-manual formats) when all matches done && finalsClinched(); response includes completed: true.
- EventAdminControls: Complete → green + ConfirmDialog with pending-match count (pendingMatches prop); Reopen amber; Start sky; emoji-free labels app-wide (stage panel, team formation, copy check-ins, self actions).
- Fun-stats row in the completed banner: biggest margin (winner), closest scoreline (pairing), most points (team).
- RosterEntry.verified (user_id present) → ✔ ticks in roster + players list.
- getPlayerStats: eventsWon via per-event champion detection (knockout clinch or group leader); profile shows 🏟 Events · 🏆 Titles · 🎯 Matches · 🎖 Wins · 📈 Win rate.
- Header shows v{pkg.version} beside the wordmark for admins (updates with every deploy build).
- EventCard displays location; StatusBadge completed is sky blue.
