# v0.9.0 — Best-of finals, rich profile, matrix multi-scores
- generateSingleElim/generatePlayoffs accept finalsBestOf (1|3|5); recordScore fills winners into every next-round series game; champion = first team to clinch majority of the finals series (page + banner logic).
- StagePanel: "Final — best of" segmented selector in the single-elim setup and the playoffs area.
- ResultsMatrix: per-pair results are lists (stacked, individually colored); dialog targets latest pending match of a pairing; repeat matchups no longer overwrite.
- MatchesSection: round-null groups rank as 0 → manual matches at the bottom with round 1.
- lib/db/stats.ts getPlayerStats: tournaments/matches/wins + recent 5 events; ProfileClient stat tiles + linked event list; skill "N / 5" text removed.
- Copy route: base name stripped of prior suffixes; uniqueness via "- N" counter (checked against all events incl. soft-deleted).
- ThemeProvider initial state "system" (Auto default everywhere).
